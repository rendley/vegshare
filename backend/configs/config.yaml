http:
  host: "0.0.0.0"       # Хост
  port: "8080"          # Порт сервера


database:
  host: "localhost"
  port: 5432
  user: "vegshare"
  dbname: "vegshare"
  password: "vegshare"
  sslmode: "disable"
#  max_connections: 10
#  timeout: 5s
#  url: "postgres://vegshare:vegshare@localhost:5432/db?sslmode=disable"

jwt:
  secret: "my_super_secret_key" # Секретный ключ для подписи токенов
  access_token_ttl: "15m"  # 15 минут
  refresh_token_ttl: "168h" # 7 дней

redis:
  url: "redis://localhost:6379"
  ttl: 3600             # TTL кеша в секундах